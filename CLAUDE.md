# iryo-joho-quiz

医療情報技師・医療情報系資格の学習用クイズアプリケーション。

## プロジェクト概要

医療情報に関する問題を出題するクイズアプリ。技術スタックが確定次第ここに追記する。

## Git 運用ルール

**コードを変更するたびに、必ずGitHubにプッシュすること。**

```bash
git add <変更ファイル>
git commit -m "変更内容の説明"
git push origin main
```

- コミットメッセージは日本語でも英語でも可。変更の意図が伝わる内容にする
- `git add .` や `git add -A` は避け、変更ファイルを明示的に指定する
- プッシュ前に `git status` で変更内容を確認する
- 作業途中でも、意味のある単位でコミット・プッシュする（WIPコミット可）
- mainブランチへの直接プッシュを基本とするが、大きな機能追加はブランチを切る

## 開発方針

- シンプルに保つ。不要な抽象化・機能の先取りをしない
- コメントは「なぜ」が非自明なときだけ書く
- セキュリティ（XSS、SQLインジェクション等）に注意する
